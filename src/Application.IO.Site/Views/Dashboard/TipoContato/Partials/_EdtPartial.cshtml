@model SystemModels.TipoContato.TipoContatoModel

<style>
    .modal-dialog {
        max-width: 500px !important;
    }
</style>

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">&times;</button>
    @if (Model.Id == 0)
    {
    <h4 class="modal-title">Inserir Tipo de Contato</h4>
    }
    else
    {
    <h4 class="modal-title">Editar - Tipo de Contato: @(Model.Nome)</h4>
    }
</div>
<div>
    @using (Html.BeginForm("SaveTipoContato", "Dashboard", FormMethod.Post, new { @class = "form-horizontal", id = "FormEdtTipoContato", role = "form", onsubmit = "return false;" }))
    {
        @Html.HiddenFor(m => m.Id)

        <div class="modal-body">
            <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12">
                    @Html.LabelFor(m => m.Nome)
                </div>
                <div class="col-lg-12 col-md-12 col-sm-12">
                    @Html.TextBoxFor(m => m.Nome, new { autocomplete = "nope", @class = "form-control", @required = "", @autofocus = "" })
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="submit" class="btn btn-success"><i class="glyphicon glyphicon-ok"></i></button>
            <button type="button" class="btn btn-danger btn-left-zero" data-dismiss="modal"><i class="glyphicon glyphicon-ban-circle"></i></button>
        </div>
    }
</div>
<script src="~/js/Views/TipoContato/dashboardEdtTipoContato.js"></script>